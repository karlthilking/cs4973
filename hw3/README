what works:
    - basic shell commands
    - pipes
    - output and input redirection
    - background jobs
    - handling SIGINT to kill shell commands but not the shell process
note:
    - make check does not test sending a SIGINT to a shell command

